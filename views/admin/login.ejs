<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/css/styles.css" />
    <title>Tela de Login</title>
    <style>

        body, html {
            height: 100%;
            margin: 0;
            font-family: 'Roboto', sans-serif;
        }

        .login-container {
            display: flex;
            height: 100vh;
        }

        .image-container {
            background: url('https://neofeed.com.br/wp-content/uploads/2023/05/agronegocio.jpg') no-repeat center center;
            background-size: cover;
            flex: 1;
        }

        .form-container {
            flex: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            background-color: #f5f5f5;
            padding: 2rem;
        }

        .form-container h1 {
            color: #28a745;
            font-size: 2.5rem;
            margin-bottom: 1rem;
        }

        .form-container p {
            color: #555;
            margin-bottom: 2rem;
        }

        .form-control {
            border: 2px solid #28a745;
            border-radius: 5px;
        }

        .btn-login {
            background-color: #28a745;
            color: white;
            font-weight: bold;
            border: none;
            padding: 0.75rem;
            border-radius: 5px;
            transition: background 0.3s ease;
        }

        .btn-login:hover {
            background-color: #1d8348;
        }

        .register-link {
            margin-top: 1rem;
            font-size: 0.9rem;
            color: #28a745;
            text-decoration: none;
        }

        .register-link:hover {
            text-decoration: underline;
        }
    </style>
  </head>
  <body>
    <div class="login-container">
      <div class="image-container"></div>

      <div class="form-container bg-success p-2 bg-opacity-10 w-100">
        <img
          src="/img/logo.png"
          class="agri2 img-thumbnail rounded-circle mx-auto d-block"
          alt=""
        />
        <h1 class="text-center text-success">Login</h1>
        <form action="/auth/login" method="POST">
          <div class="mb-3">
            <label for="username" class="form-label">Usuário</label>
            <input
              type="text"
              class="form-control"
              id="username"
              name="usuario"
              placeholder="Digite seu usuário"
              required
            />
          </div>
          <div class="mb-3">
            <label for="senha" class="form-label">Senha</label>
            <input
              type="password"
              class="form-control"
              id="password"
              name="senha"
              placeholder="Digite sua Senha"
              required
            />
          </div>
          <button type="submit" class="btn btn-success w-100">Entrar</button>
          <div class="d-grid gap-2 col-6 mx-auto">
            <a href="/" class="btn btn-secondary mt-3">Cancelar</a>
          </div>
        </form>

        <div class="text-center mt-3">
          <% if (typeof mensagem !='undefined' ) { %>
          <p class="erro"><%= mensagem %></p>
          <% } %>
        </div>
      </div>
    </div>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
